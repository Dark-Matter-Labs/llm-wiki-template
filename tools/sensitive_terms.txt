# Never-public terms — the leak tripwire.
#
# Every line below (blank lines and #-comments ignored) is matched, case-insensitively,
# against every PUBLIC surface: the whole published docs/ site AND any wiki page marked
# `public` or `unlisted`. A match fails `tools/scan_public_leaks.py` (and CI), so sensitive
# material can't reach the open web or the colleague mirror unnoticed.
#
# This is a backstop, not the main control. The main control is the ACCESS boundary
# (only Indy can open the source repo — see SHARING-AND-ACCESS.md). Add a term here
# whenever something must never appear on a public surface, even by accident.
#
# Keep entries specific enough not to fire on innocent prose. Multi-word phrases are best.

# --- July 2026 DM/XCO restructuring (private strategy) ---
reverse takeover
non-cannibalization of intellectual property
non-cannibalization of ip
legacy-option conversion
legacy DM options
winding down DM
preserving DM
asset relationship rather than a revenue relationship
greater value in a mission sense
