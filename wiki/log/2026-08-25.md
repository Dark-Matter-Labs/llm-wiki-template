# Wiki Log — 2026-08-25

Append-only record for 2026-08-25. Newest entries at the bottom. Month index: [`2026-08.md`](2026-08.md) · format: [`wiki/log.md`](../log.md).

## [2026-08-25] rebuild | Retrieval and lifecycle, carried across the federation

Two findings from indy-llm-wiki, applied here.

**Routing must cost a constant, not a proportion.** There, `index.md` had grown to ~55k tokens and
was read before every operation, against a corpus whose median page is ~1.5k — the map had grown as
fast as the territory. `tools/search.py` now ranks **title, tags and description above body text**
and excludes catalogues, so a ranked shortlist costs ~1k instead of a scan. The `query` skill routes
through it: **search, then graph, then read** — and never loads pages in order to decide which pages
to load. Body-only TF-IDF was why the tool existed for months and nothing used it: long essays
outranked the concept page a question was about.

**A staleness clock a migration cannot reset.** `tools/staleness.py` reports when a page's *body*
last changed, ignoring frontmatter-only commits. Git mtime cannot be trusted for this: one schema
backfill rewrites every page's frontmatter and resets every clock at once — in indy's wiki that made
766 pages all report the same 10 days.

**Dormancy.** `status: dormant` is now in the schema as a weight-and-visibility change, never a
deletion: the file stays readable, its row moves to an attic shelf, and `compute_gravity.py` stops
counting it so retired thinking cannot keep pulling the corpus's centre. Only a human sets it; `lint`
proposes and never applies. The lint pass also reports repo history alongside the count and declines
to present a list when history is under twice the threshold — in a young wiki "untouched 42 days"
means "arrived in the first bulk ingest", which is youth, not death.

Gravity now also skips `wiki/index/`, so the map cannot vote on where the territory's centre is.

## [2026-08-25] repair | The scale meter was measuring the wrong dimension (mine)

`tools/measure_scale.py` had drifted into every wiki by hand, carrying three defects in
every copy. Found by running it, not by reading it.

- **Coverage read `wiki/index.md` without `wiki/index/`.** Where an index is tiered, every
  shelved row was invisible.
- **The router tripwire counted lines (`> 1500`).** Measured against indy's index at its
  worst — 523 lines, ~55,000 tokens — the meter would have read "523, fine." The rows had
  grown, not multiplied, and a line count cannot see length. It now counts **tokens**
  (`> 15,000`), which is what routing actually costs.
- **It read only `[[wiki-links]]`.** This wiki catalogues with ordinary markdown paths, so
  it reported *every* page unlisted — a false alarm about a file format dressed as a
  finding about routing. Both forms now count.

Resolution goes by **path, not basename**: basenames genuinely collide across
subdirectories, and matching on them credits the wrong page while leaving the real one
counted as a gap.

`tools/test_measure_scale.py` pins all three, including an explicit assertion that the old
line tripwire would *not* have caught the fat router. Each was mutation-checked.

`measure_scale.py` now travels in the SHARED manifest rather than by hand. Also wired into
CI here: `test_measure_scale.py` and `test_staleness.py` — the latter shipped in the
2026-08-25 federation rollout without its gate, so the tool was present and unguarded.

Gates pass.

## [2026-08-25] rebuild | Cross-model checking, an editing path for humans, and the last routing gaps

**Reachability follows markdown links at both hops now.** A hub page that catalogues its
cards as `[Mapfre](accounts/mapfre.md)` could not pass reachability on to them, because the
second hop followed only `[[wiki-links]]` — so a deliberate hub was reported as a routing
failure, which is precisely what the metric's own docstring says it must not do.
Resolution is **by path, not basename**, and relative to the linking page: basenames
genuinely collide across subdirectories, and matching on them credits the wrong page.

**Cross-model checking, with the guard first.** `machine_checks:` records what a second
frontier model said about a page:

```yaml
machine_checks:
  - model: gpt-5.2
    date: 2026-08-25
    verdict: agrees | disputes | unsure
    note: one line on what it actually said
```

The rule the tooling exists to enforce:

> A machine check may move `confidence`. It may never move `validation`.

Two models agreeing is weaker evidence than it feels like — overlapping training data,
correlated failures, no stake in being wrong. The `validation` ladder is load-bearing: it
is what stops a large body of well-formed but unconfirmed material from dragging the
corpus's centre. That protection is worth exactly as much as the requirement that the rungs
above `machine` mean a person, and the shortcut that would end it is one line:
`validated_by: [gpt-5]`. `tools/cross_check.py --check` refuses anything model-shaped in
that field, and its mutation test also checks the guard is not so broad it rejects people.

`--due` queues the pages where a second read buys most: load-bearing but weakly supported.
The `cross-check` skill builds a packet a person can paste into another model — no
second-vendor key exists, and pretending otherwise would produce a skill that never runs.

**`EDITING.md` — small corrections without the chat.** GitHub's web editor already is a
CMS: pencil icon → commit to a new branch → checks run → merge, about thirty seconds, no
terminal. The guide draws the line at **blast radius, not effort**: a typo is one file; a
rename is every page that links to it. It also names the files that are computed rather
than written, because hand-editing those is the one way to make a mess that passes
inspection.

Gates pass.
