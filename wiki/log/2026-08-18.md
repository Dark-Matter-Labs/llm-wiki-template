# Log — 2026-08-18

## [2026-08-18] rebuild | House rules and the v1.0 design system, synced from indy-llm-wiki

Two rules from `xco-style-guide#11` are now first-class here: **civilization with a z**, and
**xCO** with a lowercase x. They are checked rather than remembered — `tools/house_rules.py`,
wired into this repo's CI, and stated at the top of CLAUDE.md's hard rules.

The v1.0 design system arrived with them: `design/xco.tokens.json` as the source of truth, the
generator, and the executable accessibility contract. Every wiki now carries the same shared
layer, kept that way by `tools/sync_design_system.py`.

What is deliberately NOT shared: this wiki's CLAUDE.md, its workflows, and the wording on its
social card. The design system travels; a wiki's own voice does not.
